name: Universal Build luci-app-passwall

on:
  workflow_dispatch:
    inputs:
      sdk_url:
        description: '🎯 OpenWrt SDK 下载链接 (.tar.zst)'
        required: true
        type: string
        default: 'https://downloads.openwrt.org/releases/25.12.2/targets/armsr/armv8/openwrt-sdk-25.12.2-armsr-armv8_gcc-14.3.0_musl.Linux-x86_64.tar.zst'
      arch_name:
        description: '🏷️ 架构名称 (用于输出文件命名, 如 armsr-armv8)'
        required: true
        type: string
        default: 'armsr-armv8'
      source_branch:
        description: '🌿 Passwall 源码分支'
        required: true
        type: string
        default: 'main'
      use_cache:
        description: '⚡ 启用源码缓存'
        required: true
        type: boolean
        default: true

permissions:
  contents: write

env:
  PASSWALL_PKG_REPO: "https://github.com/Openwrt-Passwall/openwrt-passwall-packages.git"
  PASSWALL_LUCI_REPO: "https://github.com/Openwrt-Passwall/openwrt-passwall.git"

jobs:
  build:
    name: Build Passwall (${{ inputs.arch_name }})
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Generate Tag Date
        id: tag_date
        run: echo "date=$(date +'%Y.%m.%d-%H%M')" >> $GITHUB_OUTPUT

      - name: Install System Dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev python3-setuptools rsync swig unzip zlib1g-dev file wget curl zstd

      - name: Cache Downloaded Sources
        if: ${{ inputs.use_cache }}
        uses: actions/cache@v4
        with:
          path: sdk_dir/dl
          key: ${{ runner.os }}-openwrt-dl-${{ inputs.arch_name }}-${{ hashFiles('**/*.yml') }}
          restore-keys: |
            ${{ runner.os }}-openwrt-dl-${{ inputs.arch_name }}-

      - name: Download and Extract OpenWrt SDK
        run: |
          echo "Downloading SDK..."
          wget -q -O sdk.tar.zst "${{ inputs.sdk_url }}"
          echo "Extracting SDK..."
          mkdir -p sdk_dir
          tar -I zstd -xf sdk.tar.zst -C sdk_dir --strip-components=1

      - name: Add Passwall Feeds
        working-directory: ./sdk_dir
        run: |
          echo "src-git passwall_packages ${{ env.PASSWALL_PKG_REPO }};${{ inputs.source_branch }}" >> feeds.conf.default
          echo "src-git passwall_luci ${{ env.PASSWALL_LUCI_REPO }};${{ inputs.source_branch }}" >> feeds.conf.default

      - name: Update and Install Feeds
        working-directory: ./sdk_dir
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Fix Kconfig & Dependency Issues
        working-directory: ./sdk_dir
        run: |
          # 修复 libcurl 递归依赖冲突 (关键修复)
          if [ -f feeds/packages/net/curl/Config.in ]; then
            echo "Applying fix for libcurl recursive dependency..."
            sed -i 's/depends on PACKAGE_libcurl//g' feeds/packages/net/curl/Config.in
          fi
          
          # 修复部分 SDK 缺失 Rust 环境导致编译失败的问题
          # 如果插件需要 rust，尝试强制禁用不需要的 Rust 依赖组件以绕过 host 编译
          # 或者在此处可以添加预装 rust 的指令

      - name: Generate Defconfig
        working-directory: ./sdk_dir
        run: |
          # 选中核心插件
          echo "CONFIG_PACKAGE_luci-app-passwall=y" >> .config
          
          # 强制选中语言包
          echo "CONFIG_LUCI_LANG_zh_Hans=y" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-passwall-zh-cn=y" >> .config
          
          # 自动处理依赖并生成最终配置
          make defconfig

      - name: Compile luci-app-passwall
        working-directory: ./sdk_dir
        run: |
          # 优先尝试多线程编译，失败则回退到单线程详细输出
          make package/luci-app-passwall/compile -j$(nproc) || make package/luci-app-passwall/compile -j1 V=s

      - name: Collect Custom APK Files
        run: |
          mkdir -p output_apk
          # 兼容 APK (新版 OpenWrt) 和 IPK (旧版)
          find sdk_dir/bin/ -type f \( -name "*.apk" -o -name "*.ipk" \) \
            \( -path "*/passwall_luci/*" -o -path "*/passwall_packages/*" \) \
            -exec cp {} output_apk/ \;

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: Passwall-${{ inputs.arch_name }}-${{ steps.tag_date.outputs.date }}
          name: 🚀 Passwall (${{ inputs.arch_name }}) - ${{ steps.tag_date.outputs.date }}
          files: output_apk/*
          body: |
            ### 自动化编译结果
            - **编译目标架构**: `${{ inputs.arch_name }}`
            - **使用的 SDK**: `${{ inputs.sdk_url }}`
            - **源码分支**: `${{ inputs.source_branch }}`
            - *修复了 libcurl 递归依赖问题。*

name: Build and Publish OpenWrt

on:
  push:
    branches: [ main ] # 当代码推送到 main 分支时自动执行
  workflow_dispatch:   # 允许在 GitHub Actions 页面手动点击触发

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 允许工作流发布 Release

    steps:
      - name: 检出当前仓库代码
        uses: actions/checkout@v4

      - name: 安装依赖环境
        run: |
          sudo apt-get update
          # 安装必要的工具，包含 git
          sudo apt-get install -y python3 curl wget git

      - name: 克隆 amlogic-s9xxx-openwrt 仓库
        run: |
          echo "正在克隆最新的 amlogic-s9xxx-openwrt 源码..."
          git clone --depth 1 https://github.com/ophub/amlogic-s9xxx-openwrt.git

      - name: 修改 imagebuilder 配置项
        working-directory: amlogic-s9xxx-openwrt
        run: |
          echo "更改 config 和 imagebuilder.sh..."
          sed -i 's/^CONFIG_PACKAGE/# &/' config/imagebuilder/config
          sed -i '$a\
          CONFIG_PACKAGE_luci-app-passwall=y\
          CONFIG_PACKAGE_luci-app-zerotier=y\
          CONFIG_PACKAGE_luci-app-ssr-plus=y\
          CONFIG_PACKAGE_luci-i18n-passwall-zh-cn=y\
          CONFIG_PACKAGE_luci-i18n-zerotier-zh-cn=y\
          CONFIG_PACKAGE_luci-i18n-ssr-plus-zh-cn=y\
          CONFIG_PACKAGE_luci-i18n-package-manager-zh-cn=y\
          CONFIG_PACKAGE_luci-i18n-firewall-zh-cn=y\
          CONFIG_PACKAGE_openssh-sftp-server=y\
          CONFIG_PACKAGE_kmod-nft-tproxy=y\
          CONFIG_PACKAGE_kmod-nft-socket=y\
          ' config/imagebuilder/config
          
          sed -i -E 's/\b(docker(-compose)?|dockerd)\b\s*//g' config/imagebuilder/imagebuilder.sh
          sed -i 's/tini ttyd tune2fs/tini tune2fs/g' config/imagebuilder/imagebuilder.sh

      - name: 注入插件下载脚本
        working-directory: amlogic-s9xxx-openwrt
        run: |
          cat > /tmp/insert_code.txt << 'EOF'
          PLUGINS=("ZeroTier" "SSR-Plus" "Passwall")
          for NAME in "${PLUGINS[@]}"; do
              echo "-----------------------------------------------"
              echo "正在搜索 $NAME 的最新发布版本..."
              
              DOWNLOAD_URLS=$(curl -s https://api.github.com/repos/Biercenter/Phicomm-N1-OpenWrt/releases | \
              python3 -c "
          import sys, json
          data = json.load(sys.stdin)
          for release in data:
              if '$NAME' in release['name']:
                  for asset in release['assets']:
                      if asset['name'].endswith('.apk'):
                          print(asset['browser_download_url'])
                  break
              ")

              if [ -z "$DOWNLOAD_URLS" ]; then
                  echo "❌ 未找到 $NAME 的相关 apk 文件。"
              else
                  echo "✅ 找到 $NAME 的文件，开始下载..."
                  echo "$DOWNLOAD_URLS" | xargs -n 1 wget -q --show-progress -nc
              fi
          done

          echo "-----------------------------------------------"
          echo "全部下载完成！文件保存在: $(pwd)"
          ls -lh *.apk
          EOF
          sed -i '/Download other luci-app-xxx/r /tmp/insert_code.txt' config/imagebuilder/imagebuilder.sh
          rm /tmp/insert_code.txt

      - name: 从 Action Secret 创建自定义设置 (uci-defaults)
        working-directory: amlogic-s9xxx-openwrt
        env:
          # 读取 GitHub 仓库配置的 Secret
          CUSTOM_SETTINGS: ${{ secrets.CUSTOM_UCI_SETTINGS }}
        run: |
          echo "添加 files/etc/uci-defaults/zzz-custom-settings"
          mkdir -p config/imagebuilder/files/etc/uci-defaults/
          # 将 Secret 的内容直接写入文件
          echo "$CUSTOM_SETTINGS" > config/imagebuilder/files/etc/uci-defaults/zzz-custom-settings
          chmod +x config/imagebuilder/files/etc/uci-defaults/zzz-custom-settings

      - name: 制作 rootfs 和打包 img
        working-directory: amlogic-s9xxx-openwrt
        run: |
          echo "开始制作 openwrt-armsr-armv8-generic-rootfs.tar.gz"
          sudo ./config/imagebuilder/imagebuilder.sh openwrt:25.12.2
          
          echo "开始打包 img 固件"
          mkdir -p openwrt-armsr/
          sudo mv output/openwrt-*-rootfs.tar.gz openwrt-armsr/openwrt-armsr-armv8-generic-rootfs.tar.gz
          sudo ./remake -b s905d -k 6.18.y -p 192.168.100.1

      - name: 收集生成的固件文件
        id: collect_firmware
        run: |
          # 使用通配符动态获取 out 目录下生成的 img.gz 路径
          echo "FIRMWARE_PATH=$(ls amlogic-s9xxx-openwrt/openwrt/out/openwrt_official_amlogic_s905d_k6.18.*.img.gz | head -n 1)" >> $GITHUB_ENV

      - name: 发布到 GitHub Releases
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "OpenWrt-Build-${{ github.run_number }}"
          name: "OpenWrt S905D 自动构建 #${{ github.run_number }}"
          files: ${{ env.FIRMWARE_PATH }}
          body: "通过 GitHub Actions 自动构建的 OpenWrt 固件。"
